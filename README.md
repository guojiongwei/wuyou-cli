## 学习搭建一个简单的脚手架

### 安装

```shell
# install it globally
$ npm install -g wuyou-cli

# or yarn
$ yarn global add wuyou-cli
```

### 使用 

#### Quick Start 

```shell
$ wuyou create <name> [-f|--force]
```

### 示例
```shell
$ wuyou create demo-app
```

#### Options

- `-f, --force`: 覆盖目录下同名文件